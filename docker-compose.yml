services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: scenius
      POSTGRES_PASSWORD: scenius
      POSTGRES_DB: scenius
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
